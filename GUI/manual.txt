instalar biblioteca flet
pip install flet

criar projeto
flet create 01_primeiro_app_flet        ## "nome do projeto"

executar o programa
flet run 01_primeiro_app_flet           ## "nome do projeto"

executar programa na web
flet run --web 01_primeiro_app_flet     ## "nome do projeto"

# criar programa executavel , com icone
flet pack 05_imc/src/main.py --name=app_imc --icon 05_imc/src/assets/1.png




=================================
o limpa
codigo base para iiciar quaquer flet                                                                                                                                        

import flet as ft

def main(page: ft.Page):

    # propriedades da pagina
    page.title = "Teste"                  # titulo da janela do programa
    page.scroll = "adaptive"                # coloca a janela em tamanho adaptavel
    page.theme_mode = ft.ThemeMode.LIGHT    # colocar o tema CLARO


    page.add(
        ft.SafeArea(
            ft.Container(

                alignment=ft.alignment.center

            ),            
            expand=True
        )
    )

ft.app(main)

